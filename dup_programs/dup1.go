//Dup1 prints the text of each line that appears more than
//once in the standard input, preceed by its count

package main

import (
	"fmt"
	"bufio"
	"os"
)

func main() {
	counts := make(map[String]int)
	input := bufio.NewScanner(os.Stdin)


}
